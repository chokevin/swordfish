# swordfish-bench-h100.yaml — H100 NVL on voice-agent-flex.
#
# DRAFT — see swordfish-bench-base.yaml header.

schema_version: 1

extends: swordfish-bench-base

metadata:
  name: swordfish-bench-h100
  description: H100 NVL swordfish benchmark on voice-agent-flex (ray/fauna-train-queue).

spec:
  scheduling:
    nodeSelector:
      gpu: h100

  env:
    SWORDFISH_ARCH_LABEL: h100
