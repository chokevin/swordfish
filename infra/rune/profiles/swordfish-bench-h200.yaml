# swordfish-bench-h200.yaml — H200 on voice-agent-flex.
#
# Capacity-gated. Run the H200 preflight (`make airun-h200-preflight`)
# before the first submit; the historical capacity blocker is
# documented at docs/airun/h200-blocker-handoff.md.
#
# DRAFT — see swordfish-bench-base.yaml header.

schema_version: 1

extends: swordfish-bench-base

metadata:
  name: swordfish-bench-h200
  description: H200 swordfish benchmark on voice-agent-flex (ray/fauna-train-queue).

spec:
  scheduling:
    nodeSelector:
      gpu: h200

  env:
    SWORDFISH_ARCH_LABEL: h200
